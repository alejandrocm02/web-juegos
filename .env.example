# ---- Servidor ----
# Puerto HTTP del servidor de juego (Express + Socket.IO)
PORT=3001
# Entorno: development | production | test
NODE_ENV=development
# Orígenes permitidos por CORS, separados por comas.
# En desarrollo el valor típico es la URL de Vite.
CORS_ORIGINS=http://localhost:5173,http://127.0.0.1:5173
# Base de datos SQLite (Prisma)
DATABASE_URL="file:./dev.db"
# Segundos que una sala vacia sobrevive antes de eliminarse
ROOM_EMPTY_TTL_SECONDS=120
# Segundos de gracia para reconectar antes de expulsar a un jugador desconectado
RECONNECT_GRACE_SECONDS=90
# Rate limiting de mensajes de socket (mensajes por ventana)
SOCKET_RATE_LIMIT=60
SOCKET_RATE_WINDOW_MS=5000
# Techo de recursos. Crear salas es la operacion mas cara del servidor: una
# partida en curso mantiene un bucle de simulacion a 60 Hz.
# Salas simultaneas que admite el proceso entero
MAX_ROOMS=500
# Conexiones simultaneas desde una misma IP. Margen amplio a proposito: detras
# de un NAT domestico o de oficina hay muchas personas con una sola IP publica.
MAX_SOCKETS_PER_IP=64
# Salas *con gente dentro* que puede tener a la vez una misma IP. Las salas
# vacias no cuentan: solo estan esperando por si alguien vuelve.
MAX_ROOMS_PER_IP=16
# Nivel de log: debug | info | warn | error
LOG_LEVEL=info

# ---- Cliente (Vite) ----
# URL del servidor de sockets. Vacio = mismo origen (usa el proxy de Vite en dev)
VITE_SERVER_URL=http://localhost:3001
